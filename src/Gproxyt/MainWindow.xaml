<ui:FluentWindow x:Class="Gproxyt.MainWindow"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:local="clr-namespace:Gproxyt"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 Title="{Binding [AppName], Source={x:Static local:AppLocalization.Current}}"
                 Icon="Assets/gproxyt.ico"
                 Width="420"
                 MinWidth="420"
                 MaxWidth="420"
                 Height="560"
                 ResizeMode="NoResize"
                 WindowStyle="None"
                 ExtendsContentIntoTitleBar="True"
                 WindowStartupLocation="CenterScreen"
                 WindowCornerPreference="Round"
                 WindowBackdropType="Mica"
                 Background="{DynamicResource ApplicationBackgroundBrush}">
    <ui:FluentWindow.Resources>
        <Style x:Key="LaunchButtonFocusVisualStyle">
            <Setter Property="Control.Template">
                <Setter.Value>
                    <ControlTemplate>
                        <Grid Margin="-3">
                            <Ellipse Stroke="{DynamicResource FocusStrokeColorOuterBrush}"
                                     StrokeThickness="2"
                                     SnapsToDevicePixels="True" />
                            <Ellipse Margin="2"
                                     Stroke="{DynamicResource FocusStrokeColorInnerBrush}"
                                     StrokeThickness="1"
                                     SnapsToDevicePixels="True" />
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </ui:FluentWindow.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="46" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0"
              Background="Transparent"
              MouseLeftButtonDown="DragWindow">
            <StackPanel Margin="16,0,0,0"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Center"
                        IsHitTestVisible="False"
                        Orientation="Horizontal">
                <Rectangle x:Name="HeaderBrandLogo"
                           Width="18"
                           Height="18"
                           Fill="#FF000000">
                    <Rectangle.OpacityMask>
                        <ImageBrush ImageSource="Assets/gproxyt.png"
                                    Stretch="Uniform" />
                    </Rectangle.OpacityMask>
                </Rectangle>
                <TextBlock x:Name="HeaderBrandTitle"
                           Margin="8,0,0,0"
                           VerticalAlignment="Center"
                           FontSize="14"
                           FontWeight="SemiBold"
                           Text="GProxyT" />
            </StackPanel>
            <StackPanel HorizontalAlignment="Right"
                        Orientation="Horizontal">
                <Button Style="{StaticResource HeaderIconButtonStyle}"
                        AutomationProperties.Name="{Binding [Settings], Source={x:Static local:AppLocalization.Current}}"
                        Click="OpenSettings">
                    <ui:SymbolIcon Symbol="Settings24"
                                   FontSize="21" />
                </Button>
                <Button Style="{StaticResource HeaderIconButtonStyle}"
                        AutomationProperties.Name="{Binding [Minimize], Source={x:Static local:AppLocalization.Current}}"
                        Click="MinimizeWindow">
                    <ui:SymbolIcon Symbol="Subtract24" FontSize="20" />
                </Button>
                <Button Style="{StaticResource HeaderIconButtonStyle}"
                        AutomationProperties.Name="{Binding [Close], Source={x:Static local:AppLocalization.Current}}"
                        Click="CloseWindow">
                    <ui:SymbolIcon Symbol="Dismiss24" FontSize="21" />
                </Button>
            </StackPanel>
        </Grid>

        <StackPanel Grid.Row="1"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center">
            <TextBlock x:Name="ContentTitle"
                       Text="GProxyT"
                       Margin="0,0,0,28"
                       HorizontalAlignment="Center"
                       FontSize="28"
                       FontWeight="SemiBold"
                       Opacity="0"
                       RenderTransformOrigin="0.5,0.5">
                <TextBlock.RenderTransform>
                    <TranslateTransform x:Name="TitleTranslate" Y="10" />
                </TextBlock.RenderTransform>
                <TextBlock.Triggers>
                    <EventTrigger RoutedEvent="Loaded">
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                 To="1"
                                                 Duration="0:0:0.28" />
                                <DoubleAnimation Storyboard.TargetName="TitleTranslate"
                                                 Storyboard.TargetProperty="Y"
                                                 To="0"
                                                 Duration="0:0:0.34">
                                    <DoubleAnimation.EasingFunction>
                                        <CubicEase EasingMode="EaseOut" />
                                    </DoubleAnimation.EasingFunction>
                                </DoubleAnimation>
                            </Storyboard>
                        </BeginStoryboard>
                    </EventTrigger>
                </TextBlock.Triggers>
            </TextBlock>
            <Button x:Name="LaunchButton"
                    Width="116"
                    Height="116"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Background="{DynamicResource AccentFillColorDefaultBrush}"
                    BorderThickness="0"
                    Cursor="Hand"
                    FocusVisualStyle="{StaticResource LaunchButtonFocusVisualStyle}"
                    AutomationProperties.Name="{Binding [LaunchChatGpt], Source={x:Static local:AppLocalization.Current}}"
                    Opacity="0"
                    RenderTransformOrigin="0.5,0.5"
                    Click="LaunchChatGpt">
                <Button.RenderTransform>
                    <ScaleTransform x:Name="LaunchScale"
                                    ScaleX="0.82"
                                    ScaleY="0.82" />
                </Button.RenderTransform>
                <Button.Triggers>
                    <EventTrigger RoutedEvent="Loaded">
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                 To="1"
                                                 Duration="0:0:0.28" />
                                <DoubleAnimation Storyboard.TargetName="LaunchScale"
                                                 Storyboard.TargetProperty="ScaleX"
                                                 To="1"
                                                 Duration="0:0:0.34">
                                    <DoubleAnimation.EasingFunction>
                                        <BackEase Amplitude="0.22" EasingMode="EaseOut" />
                                    </DoubleAnimation.EasingFunction>
                                </DoubleAnimation>
                                <DoubleAnimation Storyboard.TargetName="LaunchScale"
                                                 Storyboard.TargetProperty="ScaleY"
                                                 To="1"
                                                 Duration="0:0:0.34">
                                    <DoubleAnimation.EasingFunction>
                                        <BackEase Amplitude="0.22" EasingMode="EaseOut" />
                                    </DoubleAnimation.EasingFunction>
                                </DoubleAnimation>
                            </Storyboard>
                        </BeginStoryboard>
                    </EventTrigger>
                    <EventTrigger RoutedEvent="MouseEnter">
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetName="LaunchScale"
                                                 Storyboard.TargetProperty="ScaleX"
                                                 To="1.07"
                                                 Duration="0:0:0.15" />
                                <DoubleAnimation Storyboard.TargetName="LaunchScale"
                                                 Storyboard.TargetProperty="ScaleY"
                                                 To="1.07"
                                                 Duration="0:0:0.15" />
                            </Storyboard>
                        </BeginStoryboard>
                    </EventTrigger>
                    <EventTrigger RoutedEvent="MouseLeave">
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetName="LaunchScale"
                                                 Storyboard.TargetProperty="ScaleX"
                                                 To="1"
                                                 Duration="0:0:0.15" />
                                <DoubleAnimation Storyboard.TargetName="LaunchScale"
                                                 Storyboard.TargetProperty="ScaleY"
                                                 To="1"
                                                 Duration="0:0:0.15" />
                            </Storyboard>
                        </BeginStoryboard>
                    </EventTrigger>
                    <EventTrigger RoutedEvent="PreviewMouseLeftButtonDown">
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetName="LaunchScale"
                                                 Storyboard.TargetProperty="ScaleX"
                                                 To="0.92"
                                                 Duration="0:0:0.07" />
                                <DoubleAnimation Storyboard.TargetName="LaunchScale"
                                                 Storyboard.TargetProperty="ScaleY"
                                                 To="0.92"
                                                 Duration="0:0:0.07" />
                            </Storyboard>
                        </BeginStoryboard>
                    </EventTrigger>
                    <EventTrigger RoutedEvent="PreviewMouseLeftButtonUp">
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetName="LaunchScale"
                                                 Storyboard.TargetProperty="ScaleX"
                                                 To="1.07"
                                                 Duration="0:0:0.1" />
                                <DoubleAnimation Storyboard.TargetName="LaunchScale"
                                                 Storyboard.TargetProperty="ScaleY"
                                                 To="1.07"
                                                 Duration="0:0:0.1" />
                            </Storyboard>
                        </BeginStoryboard>
                    </EventTrigger>
                </Button.Triggers>
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Surface"
                                Background="{TemplateBinding Background}"
                                CornerRadius="58">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Surface"
                                        Property="Background"
                                        Value="{DynamicResource AccentFillColorSecondaryBrush}" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Surface"
                                        Property="Background"
                                        Value="{DynamicResource AccentFillColorTertiaryBrush}" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Surface" Property="Opacity" Value="0.5" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Button.Template>
                <Grid>
                    <ui:SymbolIcon x:Name="LaunchIcon"
                                   Symbol="Play24"
                                   Filled="True"
                                   FontSize="30"
                                   Foreground="{DynamicResource TextOnAccentFillColorPrimaryBrush}" />
                    <ui:ProgressRing x:Name="LaunchProgressRing"
                                     Width="34"
                                     Height="34"
                                     IsIndeterminate="True"
                                     Foreground="{DynamicResource TextOnAccentFillColorPrimaryBrush}"
                                     Visibility="Collapsed" />
                </Grid>
            </Button>
            <TextBlock Margin="0,16,0,0"
                       MaxWidth="340"
                       HorizontalAlignment="Center"
                       Text="{Binding [LaunchHint], Source={x:Static local:AppLocalization.Current}}"
                       TextAlignment="Center"
                       TextWrapping="Wrap"
                       FontSize="13"
                       Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
        </StackPanel>
    </Grid>
</ui:FluentWindow>
